// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// BookCategoryAssign is an object representing the database table.
type BookCategoryAssign struct {
	ID         int `boil:"id" json:"id" toml:"id" yaml:"id"`
	BookID     int `boil:"book_id" json:"book_id" toml:"book_id" yaml:"book_id"`
	CategoryID int `boil:"category_id" json:"category_id" toml:"category_id" yaml:"category_id"`

	R *bookCategoryAssignR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L bookCategoryAssignL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BookCategoryAssignColumns = struct {
	ID         string
	BookID     string
	CategoryID string
}{
	ID:         "id",
	BookID:     "book_id",
	CategoryID: "category_id",
}

// Generated where

var BookCategoryAssignWhere = struct {
	ID         whereHelperint
	BookID     whereHelperint
	CategoryID whereHelperint
}{
	ID:         whereHelperint{field: `id`},
	BookID:     whereHelperint{field: `book_id`},
	CategoryID: whereHelperint{field: `category_id`},
}

// BookCategoryAssignRels is where relationship names are stored.
var BookCategoryAssignRels = struct {
	Category string
	Book     string
}{
	Category: "Category",
	Book:     "Book",
}

// bookCategoryAssignR is where relationships are stored.
type bookCategoryAssignR struct {
	Category *BookCategory
	Book     *Book
}

// NewStruct creates a new relationship struct
func (*bookCategoryAssignR) NewStruct() *bookCategoryAssignR {
	return &bookCategoryAssignR{}
}

// bookCategoryAssignL is where Load methods for each relationship are stored.
type bookCategoryAssignL struct{}

var (
	bookCategoryAssignColumns               = []string{"id", "book_id", "category_id"}
	bookCategoryAssignColumnsWithoutDefault = []string{"book_id", "category_id"}
	bookCategoryAssignColumnsWithDefault    = []string{"id"}
	bookCategoryAssignPrimaryKeyColumns     = []string{"id"}
)

type (
	// BookCategoryAssignSlice is an alias for a slice of pointers to BookCategoryAssign.
	// This should generally be used opposed to []BookCategoryAssign.
	BookCategoryAssignSlice []*BookCategoryAssign
	// BookCategoryAssignHook is the signature for custom BookCategoryAssign hook methods
	BookCategoryAssignHook func(context.Context, boil.ContextExecutor, *BookCategoryAssign) error

	bookCategoryAssignQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	bookCategoryAssignType                 = reflect.TypeOf(&BookCategoryAssign{})
	bookCategoryAssignMapping              = queries.MakeStructMapping(bookCategoryAssignType)
	bookCategoryAssignPrimaryKeyMapping, _ = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, bookCategoryAssignPrimaryKeyColumns)
	bookCategoryAssignInsertCacheMut       sync.RWMutex
	bookCategoryAssignInsertCache          = make(map[string]insertCache)
	bookCategoryAssignUpdateCacheMut       sync.RWMutex
	bookCategoryAssignUpdateCache          = make(map[string]updateCache)
	bookCategoryAssignUpsertCacheMut       sync.RWMutex
	bookCategoryAssignUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var bookCategoryAssignBeforeInsertHooks []BookCategoryAssignHook
var bookCategoryAssignBeforeUpdateHooks []BookCategoryAssignHook
var bookCategoryAssignBeforeDeleteHooks []BookCategoryAssignHook
var bookCategoryAssignBeforeUpsertHooks []BookCategoryAssignHook

var bookCategoryAssignAfterInsertHooks []BookCategoryAssignHook
var bookCategoryAssignAfterSelectHooks []BookCategoryAssignHook
var bookCategoryAssignAfterUpdateHooks []BookCategoryAssignHook
var bookCategoryAssignAfterDeleteHooks []BookCategoryAssignHook
var bookCategoryAssignAfterUpsertHooks []BookCategoryAssignHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BookCategoryAssign) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BookCategoryAssign) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BookCategoryAssign) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BookCategoryAssign) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BookCategoryAssign) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BookCategoryAssign) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BookCategoryAssign) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BookCategoryAssign) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BookCategoryAssign) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range bookCategoryAssignAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBookCategoryAssignHook registers your hook function for all future operations.
func AddBookCategoryAssignHook(hookPoint boil.HookPoint, bookCategoryAssignHook BookCategoryAssignHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		bookCategoryAssignBeforeInsertHooks = append(bookCategoryAssignBeforeInsertHooks, bookCategoryAssignHook)
	case boil.BeforeUpdateHook:
		bookCategoryAssignBeforeUpdateHooks = append(bookCategoryAssignBeforeUpdateHooks, bookCategoryAssignHook)
	case boil.BeforeDeleteHook:
		bookCategoryAssignBeforeDeleteHooks = append(bookCategoryAssignBeforeDeleteHooks, bookCategoryAssignHook)
	case boil.BeforeUpsertHook:
		bookCategoryAssignBeforeUpsertHooks = append(bookCategoryAssignBeforeUpsertHooks, bookCategoryAssignHook)
	case boil.AfterInsertHook:
		bookCategoryAssignAfterInsertHooks = append(bookCategoryAssignAfterInsertHooks, bookCategoryAssignHook)
	case boil.AfterSelectHook:
		bookCategoryAssignAfterSelectHooks = append(bookCategoryAssignAfterSelectHooks, bookCategoryAssignHook)
	case boil.AfterUpdateHook:
		bookCategoryAssignAfterUpdateHooks = append(bookCategoryAssignAfterUpdateHooks, bookCategoryAssignHook)
	case boil.AfterDeleteHook:
		bookCategoryAssignAfterDeleteHooks = append(bookCategoryAssignAfterDeleteHooks, bookCategoryAssignHook)
	case boil.AfterUpsertHook:
		bookCategoryAssignAfterUpsertHooks = append(bookCategoryAssignAfterUpsertHooks, bookCategoryAssignHook)
	}
}

// OneG returns a single bookCategoryAssign record from the query using the global executor.
func (q bookCategoryAssignQuery) OneG(ctx context.Context) (*BookCategoryAssign, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single bookCategoryAssign record from the query.
func (q bookCategoryAssignQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BookCategoryAssign, error) {
	o := &BookCategoryAssign{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for book_category_assign")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all BookCategoryAssign records from the query using the global executor.
func (q bookCategoryAssignQuery) AllG(ctx context.Context) (BookCategoryAssignSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all BookCategoryAssign records from the query.
func (q bookCategoryAssignQuery) All(ctx context.Context, exec boil.ContextExecutor) (BookCategoryAssignSlice, error) {
	var o []*BookCategoryAssign

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BookCategoryAssign slice")
	}

	if len(bookCategoryAssignAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all BookCategoryAssign records in the query, and panics on error.
func (q bookCategoryAssignQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all BookCategoryAssign records in the query.
func (q bookCategoryAssignQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count book_category_assign rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q bookCategoryAssignQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q bookCategoryAssignQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if book_category_assign exists")
	}

	return count > 0, nil
}

// Category pointed to by the foreign key.
func (o *BookCategoryAssign) Category(mods ...qm.QueryMod) bookCategoryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.CategoryID),
	}

	queryMods = append(queryMods, mods...)

	query := BookCategories(queryMods...)
	queries.SetFrom(query.Query, "`book_category`")

	return query
}

// Book pointed to by the foreign key.
func (o *BookCategoryAssign) Book(mods ...qm.QueryMod) bookQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.BookID),
	}

	queryMods = append(queryMods, mods...)

	query := Books(queryMods...)
	queries.SetFrom(query.Query, "`book`")

	return query
}

// LoadCategory allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bookCategoryAssignL) LoadCategory(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBookCategoryAssign interface{}, mods queries.Applicator) error {
	var slice []*BookCategoryAssign
	var object *BookCategoryAssign

	if singular {
		object = maybeBookCategoryAssign.(*BookCategoryAssign)
	} else {
		slice = *maybeBookCategoryAssign.(*[]*BookCategoryAssign)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &bookCategoryAssignR{}
		}
		args = append(args, object.CategoryID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bookCategoryAssignR{}
			}

			for _, a := range args {
				if a == obj.CategoryID {
					continue Outer
				}
			}

			args = append(args, obj.CategoryID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`book_category`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BookCategory")
	}

	var resultSlice []*BookCategory
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BookCategory")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for book_category")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for book_category")
	}

	if len(bookCategoryAssignAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Category = foreign
		if foreign.R == nil {
			foreign.R = &bookCategoryR{}
		}
		foreign.R.CategoryBookCategoryAssigns = append(foreign.R.CategoryBookCategoryAssigns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CategoryID == foreign.ID {
				local.R.Category = foreign
				if foreign.R == nil {
					foreign.R = &bookCategoryR{}
				}
				foreign.R.CategoryBookCategoryAssigns = append(foreign.R.CategoryBookCategoryAssigns, local)
				break
			}
		}
	}

	return nil
}

// LoadBook allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bookCategoryAssignL) LoadBook(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBookCategoryAssign interface{}, mods queries.Applicator) error {
	var slice []*BookCategoryAssign
	var object *BookCategoryAssign

	if singular {
		object = maybeBookCategoryAssign.(*BookCategoryAssign)
	} else {
		slice = *maybeBookCategoryAssign.(*[]*BookCategoryAssign)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &bookCategoryAssignR{}
		}
		args = append(args, object.BookID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bookCategoryAssignR{}
			}

			for _, a := range args {
				if a == obj.BookID {
					continue Outer
				}
			}

			args = append(args, obj.BookID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`book`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Book")
	}

	var resultSlice []*Book
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Book")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for book")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for book")
	}

	if len(bookCategoryAssignAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Book = foreign
		if foreign.R == nil {
			foreign.R = &bookR{}
		}
		foreign.R.BookCategoryAssigns = append(foreign.R.BookCategoryAssigns, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BookID == foreign.ID {
				local.R.Book = foreign
				if foreign.R == nil {
					foreign.R = &bookR{}
				}
				foreign.R.BookCategoryAssigns = append(foreign.R.BookCategoryAssigns, local)
				break
			}
		}
	}

	return nil
}

// SetCategoryG of the bookCategoryAssign to the related item.
// Sets o.R.Category to related.
// Adds o to related.R.CategoryBookCategoryAssigns.
// Uses the global database handle.
func (o *BookCategoryAssign) SetCategoryG(ctx context.Context, insert bool, related *BookCategory) error {
	return o.SetCategory(ctx, boil.GetContextDB(), insert, related)
}

// SetCategory of the bookCategoryAssign to the related item.
// Sets o.R.Category to related.
// Adds o to related.R.CategoryBookCategoryAssigns.
func (o *BookCategoryAssign) SetCategory(ctx context.Context, exec boil.ContextExecutor, insert bool, related *BookCategory) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `book_category_assign` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"category_id"}),
		strmangle.WhereClause("`", "`", 0, bookCategoryAssignPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CategoryID = related.ID
	if o.R == nil {
		o.R = &bookCategoryAssignR{
			Category: related,
		}
	} else {
		o.R.Category = related
	}

	if related.R == nil {
		related.R = &bookCategoryR{
			CategoryBookCategoryAssigns: BookCategoryAssignSlice{o},
		}
	} else {
		related.R.CategoryBookCategoryAssigns = append(related.R.CategoryBookCategoryAssigns, o)
	}

	return nil
}

// SetBookG of the bookCategoryAssign to the related item.
// Sets o.R.Book to related.
// Adds o to related.R.BookCategoryAssigns.
// Uses the global database handle.
func (o *BookCategoryAssign) SetBookG(ctx context.Context, insert bool, related *Book) error {
	return o.SetBook(ctx, boil.GetContextDB(), insert, related)
}

// SetBook of the bookCategoryAssign to the related item.
// Sets o.R.Book to related.
// Adds o to related.R.BookCategoryAssigns.
func (o *BookCategoryAssign) SetBook(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Book) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `book_category_assign` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"book_id"}),
		strmangle.WhereClause("`", "`", 0, bookCategoryAssignPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BookID = related.ID
	if o.R == nil {
		o.R = &bookCategoryAssignR{
			Book: related,
		}
	} else {
		o.R.Book = related
	}

	if related.R == nil {
		related.R = &bookR{
			BookCategoryAssigns: BookCategoryAssignSlice{o},
		}
	} else {
		related.R.BookCategoryAssigns = append(related.R.BookCategoryAssigns, o)
	}

	return nil
}

// BookCategoryAssigns retrieves all the records using an executor.
func BookCategoryAssigns(mods ...qm.QueryMod) bookCategoryAssignQuery {
	mods = append(mods, qm.From("`book_category_assign`"))
	return bookCategoryAssignQuery{NewQuery(mods...)}
}

// FindBookCategoryAssignG retrieves a single record by ID.
func FindBookCategoryAssignG(ctx context.Context, iD int, selectCols ...string) (*BookCategoryAssign, error) {
	return FindBookCategoryAssign(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindBookCategoryAssign retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBookCategoryAssign(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*BookCategoryAssign, error) {
	bookCategoryAssignObj := &BookCategoryAssign{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `book_category_assign` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, bookCategoryAssignObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from book_category_assign")
	}

	return bookCategoryAssignObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *BookCategoryAssign) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BookCategoryAssign) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no book_category_assign provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bookCategoryAssignColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	bookCategoryAssignInsertCacheMut.RLock()
	cache, cached := bookCategoryAssignInsertCache[key]
	bookCategoryAssignInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			bookCategoryAssignColumns,
			bookCategoryAssignColumnsWithDefault,
			bookCategoryAssignColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `book_category_assign` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `book_category_assign` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `book_category_assign` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, bookCategoryAssignPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into book_category_assign")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == bookCategoryAssignMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for book_category_assign")
	}

CacheNoHooks:
	if !cached {
		bookCategoryAssignInsertCacheMut.Lock()
		bookCategoryAssignInsertCache[key] = cache
		bookCategoryAssignInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single BookCategoryAssign record using the global executor.
// See Update for more documentation.
func (o *BookCategoryAssign) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the BookCategoryAssign.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BookCategoryAssign) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	bookCategoryAssignUpdateCacheMut.RLock()
	cache, cached := bookCategoryAssignUpdateCache[key]
	bookCategoryAssignUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			bookCategoryAssignColumns,
			bookCategoryAssignPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update book_category_assign, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `book_category_assign` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, bookCategoryAssignPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, append(wl, bookCategoryAssignPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update book_category_assign row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for book_category_assign")
	}

	if !cached {
		bookCategoryAssignUpdateCacheMut.Lock()
		bookCategoryAssignUpdateCache[key] = cache
		bookCategoryAssignUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q bookCategoryAssignQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q bookCategoryAssignQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for book_category_assign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for book_category_assign")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o BookCategoryAssignSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BookCategoryAssignSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bookCategoryAssignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `book_category_assign` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bookCategoryAssignPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in bookCategoryAssign slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all bookCategoryAssign")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *BookCategoryAssign) UpsertG(ctx context.Context, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateColumns, insertColumns)
}

var mySQLBookCategoryAssignUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BookCategoryAssign) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no book_category_assign provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bookCategoryAssignColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLBookCategoryAssignUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	bookCategoryAssignUpsertCacheMut.RLock()
	cache, cached := bookCategoryAssignUpsertCache[key]
	bookCategoryAssignUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			bookCategoryAssignColumns,
			bookCategoryAssignColumnsWithDefault,
			bookCategoryAssignColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			bookCategoryAssignColumns,
			bookCategoryAssignPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert book_category_assign, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "book_category_assign", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `book_category_assign` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for book_category_assign")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == bookCategoryAssignMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(bookCategoryAssignType, bookCategoryAssignMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for book_category_assign")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for book_category_assign")
	}

CacheNoHooks:
	if !cached {
		bookCategoryAssignUpsertCacheMut.Lock()
		bookCategoryAssignUpsertCache[key] = cache
		bookCategoryAssignUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single BookCategoryAssign record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *BookCategoryAssign) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single BookCategoryAssign record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BookCategoryAssign) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BookCategoryAssign provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), bookCategoryAssignPrimaryKeyMapping)
	sql := "DELETE FROM `book_category_assign` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from book_category_assign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for book_category_assign")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q bookCategoryAssignQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no bookCategoryAssignQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from book_category_assign")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for book_category_assign")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o BookCategoryAssignSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BookCategoryAssignSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BookCategoryAssign slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(bookCategoryAssignBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bookCategoryAssignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `book_category_assign` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bookCategoryAssignPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from bookCategoryAssign slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for book_category_assign")
	}

	if len(bookCategoryAssignAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *BookCategoryAssign) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no BookCategoryAssign provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BookCategoryAssign) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBookCategoryAssign(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BookCategoryAssignSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty BookCategoryAssignSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BookCategoryAssignSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BookCategoryAssignSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bookCategoryAssignPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `book_category_assign`.* FROM `book_category_assign` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bookCategoryAssignPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BookCategoryAssignSlice")
	}

	*o = slice

	return nil
}

// BookCategoryAssignExistsG checks if the BookCategoryAssign row exists.
func BookCategoryAssignExistsG(ctx context.Context, iD int) (bool, error) {
	return BookCategoryAssignExists(ctx, boil.GetContextDB(), iD)
}

// BookCategoryAssignExists checks if the BookCategoryAssign row exists.
func BookCategoryAssignExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `book_category_assign` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if book_category_assign exists")
	}

	return exists, nil
}
